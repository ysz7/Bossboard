# BossBoard — Backend

NestJS REST API. See the [root README](../README.md) for full documentation and setup instructions.

## Quick Start

```bash
npm install
cp .env.example .env   # fill in DATABASE_URL and JWT_SECRET
npx prisma migrate deploy
npm run start:dev
```
